{
  "name": "mars-rover-bdd-kata",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cucumber": "cucumber-js --config config/cucumber.json",
    "cucumber-navigation": "cucumber-js --config config/cucumber.json --tags '@navigation'",
    "cucumber-forward": "cucumber-js --config config/cucumber.json --tags '@forward'",
    "cucumber-rotation": "cucumber-js --config config/cucumber.json --tags '@rotation'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cucumber/cucumber": "^10.8.0"
  }
}
